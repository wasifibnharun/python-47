def circle_area(rad):
    area1=3.1416*rad*rad
    print(area1)

radius=float(input("Enter the radius: "))
circle_area(radius)

def rec_area(x,y):
    area2=x*y
    print(area2)

length=float(input("Enter the length: "))
width=float(input("Enter the width: "))

rec_area(length,width)